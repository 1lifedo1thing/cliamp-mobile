# cliamp desktop

Nothing here yet.

A cliamp desktop already exists as a separate Go program; its `cmd/setup.go` is
where the declarative provider spec in
[`../android/`](../android/app/src/main/java/stream/cliamp/mobile/data/provider/ProviderSpec.kt)
was ported from. Whether this directory ends up holding that code or something
new is still open.
