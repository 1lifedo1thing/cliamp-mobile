<?xml version="1.0" encoding="utf-8"?>
<!-- Pin-dialog preview for the search widget. Static stand-ins only: dark
    ground, accent glyph, muted hint. Mirrors widget_search. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/ground_dark"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingStart="16dp"
    android:paddingEnd="16dp"
    android:paddingTop="10dp"
    android:paddingBottom="10dp">
    <ImageView
        android:layout_width="18dp"
        android:layout_height="18dp"
        android:src="@drawable/ic_w_search"
        android:tint="@color/accent" />
    <TextView
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="10dp"
        android:fontFamily="monospace"
        android:text="@string/search_widget_hint"
        android:textColor="@color/ink_tertiary"
        android:textSize="13sp" />
</LinearLayout>
